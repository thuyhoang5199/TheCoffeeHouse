import React from 'react';

export const StoreMenu = () =>
  // { menu }: { menu: string[] }
  {
    //   console.log('mmmmm', menu);

    return (
      <div>
        {/* {!isNil(menu) ? menu.map((a: string) => <h1>{a}</h1>) : <></>} */}
      </div>
    );
  };
